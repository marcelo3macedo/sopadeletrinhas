export interface FontProps {
    value?: any;
    theme?: string;
    styled?: Styles;
}

export enum Styles {
    VentiCentered,
    Huge,
    HugeCentered,
    Large,
    LargeCentered,
    Medium,
    Small,
    Default
}
